#/usr/bin/env bash

source ~/.env

BASH=$(which bash)
ZSH=$(which zsh)

USERSHELL=$(getent passwd $LOGNAME | cut -d: -f7)

if [[ "$USERSHELL" == "/usr/bin/zsh" ]]
then
	source ~/.zshrc
else
	source ~/.bash_profile
fi