#/usr/bin/env bash



BASH=$(which bash)
ZSH=$(which zsh)
USERSHELL=$(getent passwd $LOGNAME | cut -d: -f7)


$BASH ~/.env



if [ "$USERSHELL" == "/usr/bin/zsh" ]
then
	$ZSH ~/.zshrc
else
	$BASH ~/.bash_profile
fi